{
  "name": "reva-neural-hub-v2-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-frontend": "npm install --prefix language-hub",
    "build": "npm run build --prefix language-hub"
  }
}
